import 'package:flutter/material.dart';

class MediumCalibrate extends StatelessWidget {
  const MediumCalibrate({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Presque!',
      style: TextStyle(fontSize: 20, color: Colors.amber),
    );
  }
}
